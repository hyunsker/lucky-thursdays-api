목요일의 행운 (토스 미니앱 lucky-thursdays) — 핵심 파일만
================================================
※ 다음 대화에서 "이 파일 말고 건드리지 마" 식으로 범위를 줄이려면, 아래 경로만 @ 로 묶으면 됨.

[앱·토스]
  granite.config.ts     … 앱ID·브랜드·icon URL·webView
  index.html, vite.config.js
  .granite/app.json     … ait가 참조

[src — 화면·흐름]
  main.jsx, index.css, App.jsx
  components/InputScreen.jsx    … 생년월일·시간·광고 후 제출
  components/LoadingScreen.jsx
  components/ResultScreen.jsx   … 번호·공유·다음 후보
  components/GeminiLotteryDemo.jsx, AppErrorBoundary.jsx  … (있을 경우 데모/에러)

[src — 운세·Claude]
  services/claudeApi.js         … /api/fortune(워커) 호출, 타임아웃·재시도
  services/adService.js         … 전면 광고
  utils/fortunePrompt.js        … 프롬프트 (토큰·비용에 직접 영향)
  utils/displayResult.js        … JSON → 화면용 구조
  utils/sajuCalculator.js, fortuneText.js, shareFortune.js
  constants/branding.js, tossLayout.js, adIds.js

[배너·정적]
  public/branding/   … 아이콘·배너 이미지

[서버/프록시 — API 키는 여기 .env, 워커 시크릿]
  api/fortune.js           … (레포에 있을 경우) 로컬/배치용
  server/                  … dev 프록시
  cloudflare-worker/       … 프로덕션: Anthropic 키는 Cloudflare Secret

[빌드 산출 — 코드 아님]
  dist/, *.ait, *backup*.ait, *.zip  … ait build 결과·백업 (저장소에 올릴지 선택)

[기타 (미니앱 본체와 별도 실험)]
  lib/, PET_CAM_WEB_LOCATION.txt, README_FOR_CLAUDE.txt

빌드
----
  npm run build / npm run build:ait
  .env 는 .env.example 참고 (VITE_FORTUNE_API_URL 등)
